# BUTION

BUTION is a terminal application for running GGUF language models across trusted
computers on a local network with the `llama.cpp` RPC backend.

The project is under active MVP development. It does not attempt to provide
shared operating-system memory; model weights and inference work are distributed
by `llama.cpp`.

